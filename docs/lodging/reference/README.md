# 참고자료

`accommodation.xlsx`(원본 숙박관리 엑셀, 1~12월 12개 시트)를 이 폴더에 `accommodation.xlsx`로 복사해두면
`monthly-ledger` 출력(원본 재현 — 좌측 월간달력 + 우측 상세카드 + 상단 합계금액 + 사람별 숙박일수) 구현
착수 시 셀 병합/열 너비/행 높이/인쇄영역/상세카드 반복 구조를 이 파일에서 직접 분석한다.

파일이 없으면 `monthly-ledger`는 구현하지 않는다 — `docs/lodging/01-data-model.md` 참고.
